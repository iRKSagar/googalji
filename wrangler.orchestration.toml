name = "googalji-orchestration"
main = "googalji-orchestration.js"
compatibility_date = "2024-01-01"

[triggers]
crons = ["30 1 * * *"]   # 1:30 AM UTC = 7:00 AM IST daily

# Secrets — set via: wrangler secret put SECRET_NAME
# COUNCIL_SECRET   (must match BEARER constant in googalji-orchestration.js)
